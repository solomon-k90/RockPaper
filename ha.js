//(Q3).Write a javascript program to compute the sum of  two given integers.
//If the two values are thesame, then return triple their sum.
     

  //answer
    
// Define a function named sumTriple that takes two parameters,x and y function sumTriple(x, y){
    //check if x is equal to y 
//    function computeSumTriple (x, y) {
  //if (x == y){
    //if true, return three times the sum of x and y
    
    //return 3 *(x + y);
    //}else{
    //if false, return the sum of x and y 
   // return (x + y);
   // }
    
  //}
    //Log the result of calling the sumTriple function with the arguments 10 and 20 to the console
    //console.log(computeSumTriple(10,20));
    
    //Log the result of calling the sumTriple function with the arguments 10 and 10 to the console
    //console.log(computeSumTriple(10, 10));


    //function computeSumTriple(a, b){
      //if (a==b){
    //  return 3*(a + b);
      //}else {
  //    return(a + b);
  //    }
    //  }
      //console.log(computeSumTriple(20, 20));
      //console.log(computeSumTriple(100, 10));
//typing...
  "man'em fc"
  function computeSumTriple(e, a){
    if (e == a){
    return 3*(e + a);
    }else{
    return (e + a);
    }
    }
    console.log(computeSumTriple(10, 10));
    console.log(computeSumTriple(2, 8));