function checkStartsWithJava(str) {
    if (str.startsWith('Gta v')) {
    console.log('The string starts with "Gta v".');
   } else {
     console.log('The string does not start with "Gta v".');
   }
}   
   const exampleString = 'Gta v is so fun';
   checkStartsWithJava(exampleString);
   
   const anotherString = 'my friends love it too';
   checkStartsWithJava(anotherString);